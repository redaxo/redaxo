# total 1 error

parameters:
    ignoreErrors:
        -
            message: '#^Unreachable statement \- code above always terminates\.$#'
            count: 1
            path: ../../../tests/Util/TimerTest.php
